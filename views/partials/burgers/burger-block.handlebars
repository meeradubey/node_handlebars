<li>
	{{name}}

	<button class="change-devour" data-id="{{id}}" data-newdevour="{{devoured}}">
		{{#if sleep}}EAT!{{else}}Uneat!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{id}}">
		Delete
	</button>
</li>
